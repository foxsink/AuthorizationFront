export enum AuthorizationPageState {
    LOGIN = 'login',
    REGISTER = 'register',
}

export enum AuthorizationPageFormat {
    PAGE = 0,
    POPUP = 1,
}

export enum AuthorizationFieldsIdentifier {
    EMAIL = 'email',
    PASSWORD = 'password',
    NAME = 'name',
    SURNAME = 'surname',
    PHONE = 'phone',
}
